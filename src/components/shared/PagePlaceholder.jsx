function PagePlaceholder({ title }) {
  return (
    <section className="rounded-xl border border-slate-200 bg-white p-8 shadow-sm">
      <h2 className="font-sans text-3xl font-semibold text-navy">{title}</h2>
    </section>
  )
}

export default PagePlaceholder
